# 本地開發環境範例配置檔案
# 複製並重命名為 .env 後使用

# ======= 主要設定 =======

# 環境設定
ENV=development           # development, testing, production
DEBUG=true                # true, false
LOG_LEVEL=debug           # debug, info, warn, error

# 應用程式設定
APP_NAME=security-intel-platform
APP_PORT=8080
APP_HOST=0.0.0.0
API_VERSION=v1
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRY=24h           # JWT 過期時間
CORS_ALLOWED_ORIGINS=*   # * 表示所有來源，或列出特定域名 (逗號分隔)

# 資料庫設定
DB_HOST=localhost
DB_PORT=5432
DB_NAME=security_intel
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL_MODE=disable      # disable, require, verify-ca, verify-full
DB_MAX_IDLE_CONNS=10     # 最大閒置連線
DB_MAX_OPEN_CONNS=100    # 最大開啟連線數
DB_CONN_MAX_LIFETIME=1h  # 連線最大生命週期

# Redis 設定 (用於快取和分散式任務)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_PREFIX=sip:        # Redis Key 前綴

# 電子郵件設定 (開發環境用 MailHog)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@security-intel.example.com

# ======= 外部服務整合 =======

# Have I Been Pwned API
HIBP_API_KEY=your-hibp-api-key
HIBP_API_URL=https://haveibeenpwned.com/api/v3
HIBP_REQUEST_DELAY=1500  # 毫秒，避免 API 限流

# AbuseIPDB API
ABUSEIPDB_API_KEY=your-abuseipdb-api-key
ABUSEIPDB_API_URL=https://api.abuseipdb.com/api/v2

# 支付整合
STRIPE_API_KEY=sk_test_your-stripe-test-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ======= 實驗性功能 =======

# LLM 服務
LLM_API_URL=https://your-huggingface-space.hf.space/api/predict
LLM_API_KEY=your-llm-api-key
LLM_MODEL=gpt-3.5-turbo  # 或其他您選擇的模型

# IBM Quantum 整合
IBMQ_API_KEY=your-ibmq-api-key
IBMQ_PROVIDER=ibm-q-internal
IBMQ_PROJECT=your-project-id

# ======= 監控與分析 =======

# Sentry 錯誤追蹤 (選擇性)
SENTRY_DSN=your-sentry-dsn
SENTRY_ENVIRONMENT=development

# 遙測資料 (選擇性)
TELEMETRY_ENABLED=false